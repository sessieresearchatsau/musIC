// Rule 30
@import(ca.fp)

// define the rules
@compress(0);
@merge(0);
(-pl[inf] -mr[0,inf]) {
    // BB_ --> _A_;
    // BAA --> _B_;
    // AAB --> _B_;
    @decode(wns, AB, 86);
}

// Run n times
@evolve(16);
